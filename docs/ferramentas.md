# Ferramentas

CARF: Processo expõe 1 ferramenta (todas somente leitura).

### 1. `carf_processo_consultar`
**Input**: `processo` (opcional), `cpf` (opcional), `cnpj` (opcional), `data_inicio` (opcional), `data_fim` (opcional)

CARF: Processo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar CARF: Processo
```
